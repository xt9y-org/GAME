#ifndef GAME_DEPENDENCIES_DEPENDENCIES_HPP
#define GAME_DEPENDENCIES_DEPENDENCIES_HPP

#include <filesystem>

namespace Dependencies {

std::filesystem::path path(const char *name);

} // namespace Dependencies

#endif
